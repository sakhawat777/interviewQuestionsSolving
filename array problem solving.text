// MaxNumber
const arr = [1,10,12,100,90]
const maxNumber = arr.sort((a,b) => b - a)
console.log(maxNumber[0]) // Output: 100


//Find duplicates in an array using javaScript In this article we shall look at

const arry = [1, 2, 1, 3, 4, 3, 5];

const toFindDuplicates = arry => arry.filter((item, index) => arry.indexOf(item) !== index);
const duplicateElements = toFindDuplicates(arry);

console.log(duplicateElements); // Output: [1, 3]


